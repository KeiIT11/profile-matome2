import { AllSNSCardIndex } from "@/features/profiles/components/SNSCardIndex"
export default function fetchUserSNSCardIndex() {
    return (
        //データベースへの登録がまだなので、ローカルから読み込んでいる。
        AllSNSCardIndex
    )
}